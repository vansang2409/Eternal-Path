{
  "name": "mmorpg-prototype",
  "private": true,
  "version": "0.1.0",
  "description": "Playable 2D browser MMORPG prototype with Phaser, Socket.IO, Node.js, and PostgreSQL.",
  "workspaces": [
    "client",
    "server",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run build --workspace shared && npm run build --workspace server && npm run build --workspace client",
    "typecheck": "npm run build --workspace shared && npm run typecheck --workspace server && npm run typecheck --workspace client"
  },
  "engines": {
    "node": ">=20"
  }
}
